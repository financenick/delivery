# Docker commands

## Start

```bash
docker compose up --build -d
```

## Stop

```bash
docker compose stop
```

## Kill and remove containers

```bash
docker compose down
```

## Rebuild and start again

```bash
docker compose up --build -d
```
